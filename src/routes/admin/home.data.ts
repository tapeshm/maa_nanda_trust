import type { Bindings } from '../../bindings'

// [D3:page-admin-dashboard.step-02:home-exists] Home data provider placeholder; returns false until persistence is implemented.
export async function homeExists(_env: Bindings): Promise<boolean> {
  return false
}
